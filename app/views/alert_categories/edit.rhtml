h1><%= l(:title_category_edit) %></h1>

<% form_for(@category) do |f| %>
  <%= f.error_messages %>

  <%= render :partial => 'form', :locals => {:f => f}  %>

  <p>
    <%= f.submit :button_update %>
  </p>
<% end %>

<%= link_to(l(:button_back), :action => 'index') %>