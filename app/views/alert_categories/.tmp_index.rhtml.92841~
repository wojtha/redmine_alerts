<h1>Listing Alert Categories</h1>
<p>
<em>Delete operation does not care about association to alerts and reports. You must know what you are doing!</em>
</p>
<table>
<% for category in @categories %>
  <% built_in = category.id < 3 ? true : false %>
  <tr>
    <td><%=h category.name %><%= built_in ? " <em>(built-in)</em>" : '' %></td>
    <td style="color:#<%=h category.color %>">Color #<%=h category.color %></td>
    <td><%= link_to 'Edit', edit_alert_category_path(category) %></td>
    <td><%= link_to 'Delete', category, :confirm => 'Are you sure?', :method => :delete %></td>
  </tr>
<% end %>
</table>
<p>
<%= link_to 'New category', :action => 'new' %>
</p>
<p>
<%= link_to 'Back to plugin administration', :controller => :settings, :action => 'plugin', :id => :redmine_alerts %>
</p>